<UserControl x:Class="Project_bpi.Item11View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project_bpi"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="root">
    <UserControl.Resources>

        <!-- Конвертер для динамического MaxHeight -->
        <local:MaxHeightConverter x:Key="MaxHeightConverter"/>

        <!-- Стиль для Action-кнопок -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0167a4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="15"
                            Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#015a8f"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#014d78"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для кнопки Сохранить -->
        <Style x:Key="SaveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#00bf63"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="15"
                            Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#00a854"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#009147"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="20">
                <!-- Заголовок раздела -->
                <Border CornerRadius="35" 
                    Margin="10" 
                    Padding="15"
                    Height="80">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#5394ba" Offset="0"/>
                            <GradientStop Color="#0167a4" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>

                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="1. Договорная деятельность" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="White"
                               TextAlignment="Center"
                               Margin="0,0,0,10"/>

                        <TextBlock Text="1.1 Общая характеристика научных исследований" 
                               FontSize="12" 
                               FontWeight="Bold" 
                               Foreground="White"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               Margin="0,0,0,5"/>
                    </StackPanel>
                </Border>

                <!-- Основной текстовый блок с кастомным ScrollViewer -->
                <Border CornerRadius="35"
                        Margin="10"
                        Padding="15"
                        MaxHeight="{Binding ActualHeight, ElementName=root, Converter={StaticResource MaxHeightConverter}}">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#afc8d7" Offset="0"/>
                        </LinearGradientBrush>
                    </Border.Background>

                    <ScrollViewer x:Name="ContentScrollViewer"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <ScrollViewer.Resources>
                            <!-- Кастомный стиль для вертикального ScrollBar -->
                            <Style x:Key="MinimalScrollBar" TargetType="ScrollBar">
                                <Setter Property="Width" Value="6"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ScrollBar">
                                            <Grid x:Name="Bg" Background="{TemplateBinding Background}">
                                                <Track x:Name="PART_Track" 
                                                       IsDirectionReversed="true"
                                                       Width="6">
                                                    <Track.Thumb>
                                                        <Thumb Background="#80FFFFFF" 
                                                               Opacity="0.6"
                                                               Width="6">
                                                            <Thumb.Template>
                                                                <ControlTemplate TargetType="Thumb">
                                                                    <Border Background="{TemplateBinding Background}"
                                                                            CornerRadius="3"/>
                                                                </ControlTemplate>
                                                            </Thumb.Template>
                                                        </Thumb>
                                                    </Track.Thumb>
                                                </Track>
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ScrollViewer.Resources>

                        <ScrollViewer.Template>
                            <ControlTemplate TargetType="ScrollViewer">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <ScrollContentPresenter Grid.Column="0" 
                                                            Margin="{TemplateBinding Padding}"/>

                                    <ScrollBar x:Name="PART_VerticalScrollBar"
                                               Grid.Column="1"
                                               Style="{StaticResource MinimalScrollBar}"
                                               Value="{TemplateBinding VerticalOffset}"
                                               Maximum="{TemplateBinding ScrollableHeight}"
                                               ViewportSize="{TemplateBinding ViewportHeight}"
                                               Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                                </Grid>
                            </ControlTemplate>
                        </ScrollViewer.Template>

                        <!-- TextBox внутри ScrollViewer -->
                        <TextBox x:Name="ContentTextBox"
                                 FontSize="12"
                                 Foreground="Black"
                                 TextWrapping="Wrap"
                                 TextAlignment="Justify"
                                 AcceptsReturn="True"
                                 AcceptsTab="False"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 Padding="0"
                                 VerticalAlignment="Top"
                                 HorizontalAlignment="Stretch"
                                 VerticalContentAlignment="Top"
                                 IsReadOnly="True"
                                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 Text="Дать количество договоров и объем финансирования. Отразить отдельно количество и выполнение НИР по Международным, Федеральным, Региональным программам и проектам, по распоряжению ОАО «РЖД», по постановлению РАН (с указанием участия в качестве головного; ответственного по направлению или исполнителя), по грантам на научные исследования (международного, федерального уровня) с указанием объема и источника финансирования.    " />
                    </ScrollViewer>
                </Border>

                <!-- Кнопки управления -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button x:Name="EditButton" Content="Редактировать" Style="{StaticResource ActionButtonStyle}" Click="EditButton_Click"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <!-- Кнопка Сохранить в правом нижнем углу — ВСЕГДА ВИДНА -->
        <Button x:Name="SaveButton" 
                Content="Сохранить" 
                Style="{StaticResource SaveButtonStyle}"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="0,0,20,20"
                Click="SaveButton_Click"
                IsEnabled="False"/>
    </Grid>
</UserControl>